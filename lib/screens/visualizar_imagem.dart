import 'package:flutter/material.dart';

class VisualizarImagem extends StatelessWidget {
  const VisualizarImagem({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}